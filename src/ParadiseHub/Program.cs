using System;

namespace ParadiseHub
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Connected total");
            Console.WriteLine("Connected via TCP/Ethernet");
            Console.WriteLine("Connected via SSL HTTP TLS");
            Console.WriteLine("Connected via Tor");
            Console.WriteLine("Connected via Bluetooth");
            Console.WriteLine("Connected via Sneakernet");
            Console.WriteLine("Connected via VPN");
            Console.WriteLine("press ? for instructions");

            // columns
            // in pending
            // out pending
            // to location qty

            // ready to process
            // processing
            // need attn

            // rate graph (good) (error)

            // Lag time (within variance POSTAL)
        }
    }
}
